#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin>>t;
    while(t--){
        int n;
        // cin>>n;
        // vector<string> v(n);
        // for(auto &x:v)cin>>x;
        
        // int sum1=0;
        // int sum2=0;
        // for(int i=0;i<n;i++){
        //     if(v[i]=="START38"){
        //         sum1++;
        //     }else{
        //         sum2++;
        //     }
        // }
        // cout<<sum1<<" "<<sum2<<endl;

        vector<int> v;
        int n;
        cin>>n;
        for(int i=0;i,n;i++){
            int x;
            cin>>
            v.push_back(x);//.....o(1) time complexity

        }
        for(int i=0;i<v.size();i++){
            //v.size()....time complexity o(1)
            cout<<v[i]<<" ";
        }cout<<endl;
        // vector size is dynamically any thing you can put 

        vector<int> p(10,3);
        v.push_back(7);
        v.pop_back();

        vector<int> v2=v;//.........time complexity O(n)

    }
    return 0;
}